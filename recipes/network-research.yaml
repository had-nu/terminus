name: network-research

base: alpine:3.22

packages:
  - curl
  - wget
  - jq
  - dig
  - traceroute
  - tcpdump

go_tools:
  - name: wardex
    repo: github.com/had-nu/wardex/v2
    desc: Risk-based release gate — evaluate infra vulns with asset context + compensating controls
